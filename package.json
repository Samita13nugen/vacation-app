{
  "name": "python-docker",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose down && docker-compose build && docker-compose up -d",
    "stop": "docker-compose down"
  }
}
